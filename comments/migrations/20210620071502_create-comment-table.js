exports.up = function (knex) {
    let createQuery = `
            CREATE TABLE public.comment_master (
                comment_id bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY,
                post_id bigint NOT NULL,
                comment_text varchar(400) NOT NULL,
                comment_score integer NOT NULL,
                comment_created_at timestamptz(0) NOT NULL,
                comment_updated_at timestamptz(0) NOT NULL
            );
    `;
    return knex.raw(createQuery);
};

exports.down = function (knex) {
    let dropQuery = `DROP TABLE comment_db`;
    return knex.raw(dropQuery);
};
