exports.up = function (knex) {
    let createQuery = `CREATE TABLE public.feed_master (
        feed_id int8 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
        post_id int8 NOT NULL,
        post_text varchar(400) NOT NULL,
        comment_count int8 NOT NUll,
        feed_created_at timestamptz(0) NOT NULL DEFAULT now(),
        feed_updated_at timestamptz(0) NOT NULL DEFAULT now()
    );
`;
    return knex.raw(createQuery);
};

exports.down = function (knex) {};
